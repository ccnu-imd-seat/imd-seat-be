syntax = "v1"

type (
  EmptyData {}

  GeneralRes {
    Code    int       `json:"code"`
    Message string    `json:"message"`
    Data    EmptyData `json:"data"`
  }
)

@server(
    jwt: Auth
)

service seat-api {
  @handler checkIn
  get /api/checkin returns (GeneralRes)
}
